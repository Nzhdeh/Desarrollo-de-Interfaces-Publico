<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:_20_RecursosUWP.Diccionario_de_recursos">
    
    
    <LinearGradientBrush x:Key="buttonBackground3">
        <GradientStop Offset="0" Color="Red"/>
        <GradientStop Offset="1" Color="Yellow"/>
    </LinearGradientBrush>

    <Style TargetType="Button">
        <Setter Property="BorderThickness" Value="5" />
        <Setter Property="Foreground" Value="Black" />
        <Setter Property="BorderBrush" >
            <Setter.Value>
                <LinearGradientBrush StartPoint="0.5,0" EndPoint="0.5,1">
                    <GradientStop Color="Yellow" Offset="0.0" />
                    <GradientStop Color="Red" Offset="0.25" />
                    <GradientStop Color="Blue" Offset="0.75" />
                    <GradientStop Color="LimeGreen" Offset="1.0" />
                </LinearGradientBrush>
            </Setter.Value>
        </Setter>
    </Style>
    
    <!--si no le ponemos x:key se aplica a todos,
    si se lo ponemos se aplica a todos los que pongamos el style-->
    <Style TargetType="StackPanel">
        <Setter Property="Background" Value="Blue"/>
        <Setter Property="Width" Value="500"/>
    </Style>
</ResourceDictionary>
